import spinner from './spinner.gif'

const Spinner = () => (
  <img
    src={spinner}
    alt='Loading...'
    style={{ display: 'block', width: '200px', margin: 'auto' }}
  />
)

export default Spinner
